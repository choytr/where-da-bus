name: Tests

# The iOS workflow only proves the app compiles, and it skips documentation
# changes entirely — so without this, a change that breaks every test merges
# green. Deliberately ubuntu-latest and deliberately separate: none of this
# needs a Mac, and macOS runner minutes bill at 10x on private repos.

# `dev` is here but deliberately absent from ios-ipa.yml. Day-to-day work lands
# on `dev`, and the point of that branch is to reach GitHub without spending
# macOS runner minutes — but "no .ipa build" must not mean "no tests", or `dev`
# becomes a place where breakage accumulates unseen until it merges.
on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # `npm run test:scripts` runs node:test over node:sqlite, which the
          # GTFS build script and its tests both need.
          node-version: 22
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Component and unit tests
        run: npm test

      - name: Build-script and SQL tests
        run: npm run test:scripts

      - name: Typecheck
        run: npm run typecheck
