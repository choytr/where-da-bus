name: Build unsigned iOS IPA

# Produces an unsigned .ipa for sideloading via SideStore/AltStore with a free
# Apple ID. No paid Apple Developer account and no Mac are required — signing
# happens on-device at sideload time, not here.

on:
  push:
    branches: [main]
    # macOS runners bill at 10x on private repos. Documentation changes cannot
    # affect the build, so they must not consume the monthly minute budget.
    paths-ignore:
      - 'docs/**'
      - '**.md'
  workflow_dispatch:

concurrency:
  group: ios-ipa-${{ github.ref }}
  cancel-in-progress: true

env:
  # `.env` is gitignored, so the runner has no AppID unless one is put here.
  # Without it the app builds and installs perfectly and then answers every
  # arrivals request with "Invalid or unspecified API key" — a failure that
  # cannot happen in Expo Go, which reads the developer's local `.env`.
  #
  # `EXPO_PUBLIC_` values are inlined into the bundle at build time, so this
  # has to be present during `xcodebuild`, not merely at runtime.
  #
  # The AppID is not a secret in the cryptographic sense — it ships inside
  # every .ipa and is extractable from one, which the design accepts. It lives
  # in GitHub secrets so it stays out of the repository, not because storing
  # it there makes it private.
  EXPO_PUBLIC_THEBUS_APP_ID: ${{ secrets.EXPO_PUBLIC_THEBUS_APP_ID }}

jobs:
  build:
    # SDK 54 / React Native 0.81 builds on Xcode 16.x, which macos-15 ships
    # (16.4). The previous macos-26 pin existed only because Expo SDK 57's
    # ExpoModulesJSI required Swift tools 6.2; the downgrade to 54 on
    # 2026-08-01 removed that constraint, and 16.4 is the better-trodden
    # toolchain for RN 0.81 than Xcode 26 is.
    runs-on: macos-15
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      # Fail in five seconds rather than after a 4-minute build, a download and
      # a sideload. The missing-AppID build is indistinguishable from a good one
      # until you tap a stop on a physical phone, which is the most expensive
      # place in this project to discover anything.
      - name: Require the AppID
        run: |
          if [ -z "$EXPO_PUBLIC_THEBUS_APP_ID" ]; then
            echo "::error::EXPO_PUBLIC_THEBUS_APP_ID is not set."
            echo "The .ipa would build fine and then fail every arrivals request"
            echo "with 'Invalid or unspecified API key'. Set it with:"
            echo "  gh secret set EXPO_PUBLIC_THEBUS_APP_ID"
            exit 1
          fi
          echo "AppID present (${#EXPO_PUBLIC_THEBUS_APP_ID} characters)."

      - name: Select newest available Xcode
        run: |
          ls -d /Applications/Xcode*.app
          XCODE=$(ls -d /Applications/Xcode_*.app 2>/dev/null | sort -V | tail -1)
          if [ -n "$XCODE" ]; then sudo xcode-select -s "$XCODE"; fi
          xcodebuild -version
          swift --version

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install JS dependencies
        run: npm ci

      - name: Generate native iOS project
        run: npx expo prebuild --platform ios --clean

      - name: Resolve workspace and scheme
        id: xc
        working-directory: ios
        run: |
          WORKSPACE=$(find . -maxdepth 1 -name "*.xcworkspace" | head -1)
          # CocoaPods registers a scheme per pod target, so the workspace scheme
          # list is mostly dependencies. Expo prebuild names the app scheme after
          # the workspace itself — derive it that way, then verify it exists.
          SCHEME=$(basename "$WORKSPACE" .xcworkspace)
          xcodebuild -list -json -workspace "$WORKSPACE" | node -e "
            let s = '';
            process.stdin.on('data', d => s += d).on('end', () => {
              const want = process.argv[1];
              const schemes = JSON.parse(s).workspace.schemes;
              if (!schemes.includes(want)) {
                console.error('Scheme ' + want + ' not found. Available: ' + schemes.join(', '));
                process.exit(1);
              }
            });
          " "$SCHEME"
          echo "workspace=$WORKSPACE" >> "$GITHUB_OUTPUT"
          echo "scheme=$SCHEME" >> "$GITHUB_OUTPUT"
          echo "Resolved workspace=$WORKSPACE scheme=$SCHEME"

      - name: Build unsigned .app
        working-directory: ios
        run: |
          xcodebuild \
            -workspace "${{ steps.xc.outputs.workspace }}" \
            -scheme "${{ steps.xc.outputs.scheme }}" \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGN_ENTITLEMENTS="" \
            build

      - name: Package .ipa
        run: |
          APP=$(find ios/build/Build/Products/Release-iphoneos -maxdepth 1 -name "*.app" | head -1)
          if [ -z "$APP" ]; then
            echo "No .app produced — build failed silently"
            exit 1
          fi
          # Name the .ipa after the built .app rather than hardcoding it, so a
          # rename in app.json cannot leave this step pointing at a stale name.
          NAME=$(basename "$APP" .app)
          echo "Packaging $APP as $NAME.ipa"
          mkdir -p Payload
          cp -R "$APP" Payload/
          zip -qry "$NAME.ipa" Payload
          ls -lh "$NAME.ipa"

      - name: Upload .ipa artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-ipa
          path: "*.ipa"
          if-no-files-found: error
          retention-days: 30
