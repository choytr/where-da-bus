import { SettingsScreen } from '../../features/settings/SettingsScreen';

/** `/settings` — appearance, feed status, and the legal small print. */
export default function SettingsTab() {
  return <SettingsScreen />;
}
